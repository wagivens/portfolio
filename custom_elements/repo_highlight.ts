import {html, css, LitElement} from 'lit';
import {customElement, property} from 'lit/decorators.js';

@customElement('repo-highlight')
export class RepoHighlight extends LitElement {

    static styles = css`
    
    `;
    
    render() {
        return html`
        `
    }
}